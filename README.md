# PhoneContact-BinarySearch
A console-based Phone Contact Manager in Java that uses Binary Search for O(log n) name lookups. Displays each search step (low, mid, high indices) for learning purposes. Features sorted insertion, phone number validation, and duplicate prevention. DAA Lab — Binary Search Demo.
